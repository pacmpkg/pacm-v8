//! Helper crate to exercise the pacm-v8 bindings.

#[cfg(test)]
mod tests {
    #[test]
    fn smoke_test() {
        // Placeholder so `cargo test` without integration tests still runs.
        assert_eq!(2 + 2, 4);
    }
}
